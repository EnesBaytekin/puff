# Frontend UI Dockerfile
# Multi-stage build for optimal image size

# Stage 1: Build stage (not needed for static files, but kept for consistency)
FROM node:20-alpine AS builder

WORKDIR /app

# Copy package files
COPY package*.json ./

# Install dependencies (only production)
RUN npm ci --only=production

# Copy all files
COPY . .

# Stage 2: Production stage with nginx
FROM nginx:alpine

# Copy static files to nginx
COPY --from=builder /app /usr/share/nginx/html

# Fix permissions for nginx to read files
RUN chmod -R 644 /usr/share/nginx/html/js/*.js && \
    chmod -R 755 /usr/share/nginx/html/js && \
    find /usr/share/nginx/html -type d -exec chmod 755 {} \;

# Copy custom nginx config
COPY nginx.conf /etc/nginx/conf.d/default.conf

# Expose port 80
EXPOSE 80

# Health check
HEALTHCHECK --interval=30s --timeout=3s --start-period=5s --retries=3 \
    CMD wget --no-verbose --tries=1 --spider http://localhost/ || exit 1

# Start nginx
CMD ["nginx", "-g", "daemon off;"]
